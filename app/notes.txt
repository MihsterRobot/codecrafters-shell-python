# These are the commands a user can enter: "type","echo", and "exit"

# Examples of invalid inputs: 
# type "invalid_input"
# echo "invalid_input"
# "invalid_input"

In this program, so far, all invalid inputs include underscores.

1. Identify what command the user wants to run by grabbing the first word 
2. If there's no command and only an invalid input, print "not found"
3. If the command is valid but followed by invalid input, print "not found"
4. Otherwise, use the dict to grab the associated command, then print the result


